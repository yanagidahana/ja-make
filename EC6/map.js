var doubledNumbers = [];
doubledNumbers.push(number[i]*2);
return number * 2;
doubledNumbers;

var cars = [
  {type: '軽自動車', price: '安い'},
  {type:'高級車', price: '高い'}
];

var price = cars.map(function(car) {
  return car.price;
});

prices;